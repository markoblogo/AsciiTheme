# React example

Minimal React / Next.js App Router integration for `@abvx/ascii-theme/react`.

Files:

- `AsciiThemeBoot.tsx`: client boot component plus a small inspector using `useAsciiTheme()`

Usage:

```tsx
import { AppAsciiThemeBoot, ThemeInspector } from "./AsciiThemeBoot";
```
